import React from "react";

const main = {
    header: {
        textAlign: "left",
    },

    wrapper: {
        display: "flex",
        alignItems: "flex-start",
    },

    
}

export default main;